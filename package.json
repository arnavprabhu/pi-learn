{
  "name": "pi-learn",
  "private": true,
  "type": "module",
  "description": "Project-local adaptive learning for Pi",
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "reset": "node scripts/reset.mjs"
  },
  "dependencies": {
    "unpdf": "^1.8.1"
  }
}
